# /paper-sync

Sync **[screen name]** from **this repo’s implementation** into **Paper** so the frame matches what the code renders.

**Do this:** Follow **`.cursor/skills/paper-sync/SKILL.md`** end-to-end (Paper MCP + checkpoints, or paste mode if MCP is off).

**I’ll provide (or the agent should ask for):** Paper file/page; existing artboard + parent frame for inserts (or new artboard + target size); code paths; token/theme file paths.

**Defaults:** prefer existing artboard; dedicated inner “spec from code” frame; chunked `write_html`; copy from code only; **`TBD — verify in code`** when unknown.
